{% extends "base.html.twig" %}

{% block body %}
    <div class="container mt-5">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Add program</button>
        {{ form_end(form) }}
    </div>
{% endblock %}